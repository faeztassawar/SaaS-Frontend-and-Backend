"use client";

import { MdSearch } from "react-icons/md";

type SearchProps = {
  placeholder: string;
};

const Search = ({ placeholder }: SearchProps) => {
  return <></>;
};

export default Search;
